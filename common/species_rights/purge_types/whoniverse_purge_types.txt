purge_whoniverse_dalek_conversion = {
	pop_escape_chance = 0.20
	pop_decline_rate = 25 # 1 pop per 4 months, overrides BASE_POP_DECLINE, will only work on purge types

	pop_modifier = {
		pop_happiness = -0.5
	}

	potential = {
		exists = from
		from = {
			is_whoniverse_dalek = yes
			has_ascension_perk = ap_whoniverse_dalek
		}
	}

	allow = {
		hidden_trigger = { exists = from }
		# custom_tooltip = {
		# 	fail_text = CANNOT_NECROPHAGE_MACHINES
		# 	is_robotic = no
		# }
		custom_tooltip = {
				fail_text = WHONIVERSE_CANNOT_CONVERT_NONHUMANS
				is_whoniverse_human_species = yes
			}
		if = {
			limit = { is_lonely_hive_mind_pop = no }
			custom_tooltip = {
				fail_text = PURGES_ALLOWED
				from = {
					OR = {
						has_valid_civic = civic_fanatic_purifiers
						has_policy_flag = purge_allowed
					}
				}
			}
		}
	}
	ai_will_do = {
		factor = 400
	}
}